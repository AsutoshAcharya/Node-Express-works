const sayHi = require("./App");
const names = require("./3-names");
console.log(names);
require("./alternative"); //directly accessing the js file that has function call
// sayHi("asu");
// sayHi(names.ram);
// sayHi(names.shyam);
